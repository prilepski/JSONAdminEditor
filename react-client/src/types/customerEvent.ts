// Legacy file - types moved to organized modules
// Re-export for backward compatibility
export type { 
  EventData as CustomerEventData,
  EventField,
  TemplateField,
  ContentVariable 
} from './events';